# =============================================================================
# Render Blueprint — Infrastructure as Code
# Deploy: https://render.com/docs/blueprint-spec
# =============================================================================

databases:
  - name: invoice-tracker-db
    databaseName: invoice_tracker
    plan: free
    ipAllowList: [] # allow only from Render services

services:
  - type: web
    name: invoice-tracker-api
    env: node
    plan: free
    region: oregon
    buildCommand: npm run install:all && npm run build
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: invoice-tracker-db
          property: connectionString
      - key: DB_SSL
        value: "true"
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 24h
      - key: CORS_ORIGIN
        value: https://invoice-tracker-api.onrender.com
      - key: TRUST_PROXY
        value: "true"
      - key: RATE_LIMIT_MAX
        value: "200"
      - key: PORT
        value: "10000"
